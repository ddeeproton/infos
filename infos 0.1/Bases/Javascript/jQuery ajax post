$.post("ajax.php", {
	myvarname1:1, 
	myvarname2:'add_product' 
})
.done(function( data ) {
	alert(data);
})
.fail(function() {
	alert( "error" );
})
.always(function() {
	//alert( "finished" );
});
